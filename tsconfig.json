{
  "compilerOptions": {
    // --- Nossas Configurações de Layout ---
    "rootDir": "./src",
    "outDir": "./public/js",

    // --- Configurações de Ambiente ---
    "module": "amd",
    "target": "es2018",
    "outFile": "./public/js/app.js",

    // --- Outras Saídas ---
    // Ajuda a depurar o código .ts diretamente no navegador
    "sourceMap": true,

    // --- Opções de Verificação Estrita (do seu arquivo) ---
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,

    // --- Opções Recomendadas (do seu arquivo) ---
    "strict": true,
    // "verbatimModuleSyntax": true,
    // "isolatedModules": true,
    "noUncheckedSideEffectImports": true,
    // "moduleDetection": "force",  
    "skipLibCheck": true
    // (Removi a opção "jsx" pois não estamos usando React)
  },
  "include": [
    "src"
  ]
}